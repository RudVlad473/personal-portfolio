import bottomSizeVars from "../../scss/bottom-size.module.scss"

export const bottomWidth = parseInt(bottomSizeVars.bottomWidth)

export const bottomHeight = parseInt(bottomSizeVars.bottomHeight)

export const bottomScale = parseInt(bottomSizeVars.bottomScale)
